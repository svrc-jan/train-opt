{
	"tasks": [
		{
			"label": "Build all debug",
			"type": "shell",
			"command": "cmake . && cmake --build . --target all -j 8"
			,
			"group": {
				"kind": "build",
				"isDefault": true
			}
		},
	],
	"version": "2.0.0"
}
